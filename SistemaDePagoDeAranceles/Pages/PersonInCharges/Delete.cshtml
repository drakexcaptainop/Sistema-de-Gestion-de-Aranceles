@page "{id?}"
@model SistemaDePagoDeAranceles.Pages.PersonInCharges.DeleteModel
@{
    ViewData["Title"] = "Eliminar Encargado";
}

<h2>¿Seguro que quieres eliminar este encargado?</h2>

<div>
    <strong>@Model.Person.FirstName @Model.Person.LastName</strong><br />
    <span>@Model.Person.Email</span><br />
    <span>@Model.Person.Phone</span>
</div>

<form method="post" class="mt-3">
    <input type="hidden" asp-for="Person.Id" />
    <button type="submit" class="btn btn-danger">Eliminar</button>
    <a asp-page="Index" class="btn btn-secondary">Cancelar</a>
</form>